# Authentication Patterns in SPAs

## Token-Based Auth

Most SPAs use **JWT** or **opaque tokens** for authentication. The token is included in API requests via the `Authorization` header.

## Secure Token Storage

Options for storing auth tokens, ranked by security:

1. **HttpOnly cookie** (set by server): most secure, immune to XSS
2. **In-memory variable**: secure but lost on refresh
3. **sessionStorage**: vulnerable to XSS, cleared on tab close
4. **localStorage**: vulnerable to XSS, persists indefinitely

## Recommended Flow

```
1. User submits credentials
2. Server validates and sets HttpOnly cookie
3. Browser automatically includes cookie in subsequent requests
4. Server validates cookie on each request
5. Refresh token rotation extends sessions
```

## Auth Provider Pattern

```tsx
function AuthProvider({ children }: { children: React.ReactNode }) {
  const [user, setUser] = useState<User | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    checkSession().then(setUser).finally(() => setLoading(false));
  }, []);

  if (loading) return <FullPageSpinner />;

  return (
    <AuthContext.Provider value={{ user, login, logout }}>
      {children}
    </AuthContext.Provider>
  );
}
```

## Protected Routes

Wrap authenticated routes with a guard that redirects unauthenticated users to the login page. Store the original URL to redirect back after login.

See [[fe-049]] for Edge Middleware auth checks.
