# AWS VPC Endpoints

## What Are VPC Endpoints

**VPC Endpoints** let you privately connect your VPC to AWS services without traversing the internet. Traffic stays on the AWS network, improving security and reducing NAT gateway costs.

## Types

### Gateway Endpoints

Free. Support S3 and DynamoDB only:

```hcl
resource "aws_vpc_endpoint" "s3" {
  vpc_id       = aws_vpc.main.id
  service_name = "com.amazonaws.us-east-1.s3"
  route_table_ids = aws_route_table.private[*].id
}
```

### Interface Endpoints

Cost per hour + per GB. Support most AWS services via PrivateLink:

```hcl
resource "aws_vpc_endpoint" "ecr_api" {
  vpc_id              = aws_vpc.main.id
  service_name        = "com.amazonaws.us-east-1.ecr.api"
  vpc_endpoint_type   = "Interface"
  subnet_ids          = aws_subnet.private[*].id
  security_group_ids  = [aws_security_group.vpc_endpoints.id]
  private_dns_enabled = true
}
```

## Essential Endpoints for EKS

- **ECR** (ecr.api + ecr.dkr) — pull container images without NAT
- **S3** — access state files, backups, logs
- **STS** — IAM role assumption for IRSA
- **CloudWatch Logs** — ship logs without NAT
- **Secrets Manager** — fetch secrets without NAT

## Cost Impact

VPC endpoints for S3 alone can save significant NAT gateway data processing costs if your workloads transfer large amounts of data to/from S3.

See [[ops-009]] for VPC design, [[ops-062]] for networking fundamentals.
