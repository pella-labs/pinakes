---
title: PostgreSQL Window Functions
tags: [postgresql, sql, analytics]
---

# Window Functions

## What Are Window Functions?

**Window functions** perform calculations across a set of rows related to the current row, without collapsing them into a single output row like GROUP BY does. They're essential for analytics queries.

## Common Window Functions

```sql
-- Running total
SELECT
  date,
  revenue,
  sum(revenue) OVER (ORDER BY date) AS running_total
FROM daily_revenue;

-- Rank within groups
SELECT
  department,
  name,
  salary,
  rank() OVER (PARTITION BY department ORDER BY salary DESC) AS dept_rank
FROM employees;

-- Moving average
SELECT
  date,
  value,
  avg(value) OVER (
    ORDER BY date
    ROWS BETWEEN 6 PRECEDING AND CURRENT ROW
  ) AS moving_avg_7d
FROM metrics;
```

## Window Frame Specification

The frame defines which rows are included in the window:

- `ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW` — all rows up to current
- `ROWS BETWEEN 3 PRECEDING AND 3 FOLLOWING` — sliding window of 7 rows
- `RANGE BETWEEN INTERVAL '1 hour' PRECEDING AND CURRENT ROW` — time-based window

## Performance

Window functions scan data once, making them more efficient than correlated subqueries. However, large windows over unindexed columns can be slow. Consider materialized views (see [[db-040]]) for frequently-run window queries.
