# Zod for Runtime Validation

## Why Runtime Validation

TypeScript types disappear at runtime. Data from APIs, forms, URL params, and localStorage needs runtime validation to catch malformed data before it causes bugs deep in the component tree.

## Zod Basics

```typescript
import { z } from 'zod';

const UserSchema = z.object({
  id: z.string().uuid(),
  name: z.string().min(1).max(100),
  email: z.string().email(),
  role: z.enum(['admin', 'editor', 'viewer']),
  createdAt: z.string().datetime(),
});

type User = z.infer<typeof UserSchema>;

// Runtime validation
function parseUser(data: unknown): User {
  return UserSchema.parse(data);
}
```

## API Response Validation

```typescript
async function fetchUser(id: string): Promise<User> {
  const response = await fetch(`/api/users/${id}`);
  const data = await response.json();
  return UserSchema.parse(data);
}
```

## Integration with Forms

Zod integrates with react-hook-form via `@hookform/resolvers/zod`. See [[fe-014]] for form handling patterns.

## Integration with Server Actions

Validate form data in Next.js Server Actions:

```typescript
'use server';
export async function createUser(formData: FormData) {
  const result = UserSchema.safeParse(Object.fromEntries(formData));
  if (!result.success) return { errors: result.error.flatten() };
  await db.user.create({ data: result.data });
}
```

See [[fe-046]] for Server Actions and [[fe-035]] for TypeScript patterns.
