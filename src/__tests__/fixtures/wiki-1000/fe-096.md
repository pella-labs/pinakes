# Intersection Observer Patterns

## What It Does

The **Intersection Observer API** efficiently detects when an element enters or exits the viewport. No scroll event listeners needed—it uses a browser-level observer.

## Lazy Loading Images

```typescript
function useLazyImage(src: string) {
  const imgRef = useRef<HTMLImageElement>(null);
  const [loaded, setLoaded] = useState(false);

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          if (imgRef.current) imgRef.current.src = src;
          setLoaded(true);
          observer.disconnect();
        }
      },
      { rootMargin: '200px' }
    );

    if (imgRef.current) observer.observe(imgRef.current);
    return () => observer.disconnect();
  }, [src]);

  return { imgRef, loaded };
}
```

## Infinite Scroll

Place a sentinel element at the end of the list. When it becomes visible, load more data:

```tsx
function InfiniteList({ loadMore }) {
  const sentinelRef = useRef(null);

  useEffect(() => {
    const observer = new IntersectionObserver(([entry]) => {
      if (entry.isIntersecting) loadMore();
    });

    if (sentinelRef.current) observer.observe(sentinelRef.current);
    return () => observer.disconnect();
  }, [loadMore]);

  return <div ref={sentinelRef} aria-hidden="true" />;
}
```

## Other Uses

- Triggering animations when elements scroll into view
- Tracking content visibility for analytics
- Sticky header state changes
- Parallax scroll effects

See [[fe-040]] for virtual scrolling and [[fe-087]] for Astro's `client:visible` directive.
